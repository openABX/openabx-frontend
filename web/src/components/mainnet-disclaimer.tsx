'use client'

// Secondary mainnet disclaimer sits just under the AlphaBanner. The new
// design folds most messaging into the AlphaBanner, so this is minimal.

export function MainnetDisclaimer() {
  return null
}
